import * as types from '../../actions/types';

const initialState = {};

export default function messageState(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
